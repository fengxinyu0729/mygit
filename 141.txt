先执行前两条，重启，如果不行，执行这3条重启服务。
 DELETE FROM MD_SYSINFO S WHERE S.MASTERTABLE NOT IN (SELECT  MBNAME  FROM B0101_MAINBODY );

 DELETE FROM MD_ORG_VERSION T WHERE T.TABLENAME  NOT IN (SELECT K.MBNAME FROM B0101_MAINBODY K);

 DELETE FROM B0102_MASTERDATADEFINE M WHERE M.NAME NOT IN (SELECT K.MBNAME FROM B0101_MAINBODY K);