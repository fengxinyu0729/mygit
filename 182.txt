查询脏数据：以2个版本为例：
SELECT T.RECID, T.STDCODE, T.STDNAME, 'MD_ORG_JDKBJ    'FROM MD_ORG_JDKBJ     T WHERE T.RECID NOT IN (SELECT M.RECID FROM MD_ORG M) UNION ALL
SELECT T.RECID, T.STDCODE, T.STDNAME, 'MD_ORG_KS    'FROM MD_ORG_KS      T WHERE T.RECID NOT IN (SELECT M.RECID FROM MD_ORG M)
删除各版本脏数据，重启服务即可DELETE  FROM MD_ORG_JDKBJ    T WHERE T.RECID NOT IN (SELECT M.RECID FROM MD_ORG M) ;
