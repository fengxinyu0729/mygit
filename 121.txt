1 新建一个指标 ZB2 然后把 ZB的值用数据库里 复制到 ZB2 ， 貌似JQR公式也不行。得数据库操作。
2 把ZB字段删了
如果你的字段标识可以改的话，这里就结束了，否则看下面：
2 不要在DNA下删除ZB,去数据库删除ZB字段
3 数据库里把ZB2改为ZB , 数据库里操作，DNA也不支持改标识
4 然后去B0101_MAINBODY, CORE_METADATA里对应表的XML定义里改ZB精度。